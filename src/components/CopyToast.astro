<div class="copy-toast" id="copyToast">
  <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
    <polyline points="20 6 9 17 4 12"></polyline>
  </svg>
  <span id="copyToastText">Precio copiado</span>
</div>

<style>
  .copy-toast {
    position: fixed;
    bottom: 32px;
    left: 50%;
    transform: translateX(-50%) translateY(20px);
    background: var(--cfv-accent);
    color: var(--cfv-bg);
    padding: 12px 20px;
    border-radius: 10px;
    font-size: 13px;
    font-weight: 700;
    box-shadow: 0 10px 30px rgba(var(--cfv-accent-rgb), 0.3);
    opacity: 0;
    pointer-events: none;
    transition: all 0.3s ease;
    z-index: 1;
    display: flex;
    align-items: center;
    gap: 8px;
  }
  :global([data-theme="light"]) .copy-toast {
    color: var(--cfv-bg);
  }

  .copy-toast:global(.show) {
    opacity: 1;
    transform: translateX(-50%) translateY(0);
  }
</style>
