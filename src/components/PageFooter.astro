<footer class="footer">
  <p class="footer-text">
    <span class="footer-accent">Lempira</span>
    &middot; Calculadora de precios &middot; Lempiras (HNL)
  </p>
  <p class="footer-copy">
    &copy; {new Date().getFullYear()}
    <a
      href="https://cfv.technology"
      target="_blank"
      rel="noopener noreferrer"
      class="footer-link"
    >CFV Technology</a>
    &middot; Todos los derechos reservados
  </p>
</footer>

<style>
  .footer {
    margin-top: 48px;
    padding-top: 28px;
    border-top: 1px solid rgba(var(--cfv-accent-rgb), 0.1);
    text-align: center;
    display: flex;
    flex-direction: column;
    gap: 6px;
  }

  :global([data-theme="light"]) .footer {
    border-top-color: var(--cfv-border-subtle);
  }

  .footer-text,
  .footer-copy {
    margin: 0;
    font-size: 11px;
    font-weight: 500;
    letter-spacing: 1px;
    text-transform: uppercase;
    color: var(--cfv-neutral);
    line-height: 1.5;
  }

  .footer-accent {
    color: var(--cfv-accent);
    font-weight: 700;
  }

  :global([data-theme="light"]) .footer-accent {
    color: var(--cfv-primary);
  }

  .footer-link {
    color: var(--cfv-accent);
    text-decoration: none;
    font-weight: 700;
    position: relative;
    transition: color 0.2s ease;
  }

  .footer-link::after {
    content: "";
    position: absolute;
    left: 0;
    bottom: -1px;
    width: 100%;
    height: 1px;
    background: var(--cfv-accent);
    transform: scaleX(0);
    transform-origin: left;
    transition: transform 0.2s ease;
  }

  .footer-link:hover::after {
    transform: scaleX(1);
  }

  :global([data-theme="light"]) .footer-link {
    color: var(--cfv-primary);
  }

  :global([data-theme="light"]) .footer-link::after {
    background: var(--cfv-primary);
  }

  .footer-link:focus-visible {
    outline: none;
    box-shadow: 0 0 0 2px rgba(var(--cfv-accent-rgb), 0.4);
    border-radius: 2px;
  }

  :global([data-theme="light"]) .footer-link:focus-visible {
    box-shadow: 0 0 0 2px rgba(var(--cfv-accent-rgb), 0.3);
  }
</style>
